<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             xmlns:local="clr-namespace:MauiApp1.CustomCode">

    <ContentPage.Resources>
        <local:DrawingCanvas x:Key="canvas"/>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout
            Padding="10"
            WidthRequest="500"
            HorizontalOptions="Center">

            <GraphicsView 
                x:Name="Canvas"
                HorizontalOptions="Fill"
                Drawable="{StaticResource canvas}" 
                HeightRequest="500"/>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
